import { Grid, Container } from "@mui/material";

const Contact = () => {
  return (
    <section>
      <Container sx={{ mt: 10 }}>
        <Grid>
          <h1>Contact</h1>
        </Grid>
      </Container>
    </section>
  );
};

export default Contact;
